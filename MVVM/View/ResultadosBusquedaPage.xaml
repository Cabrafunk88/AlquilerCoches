<Page x:Class="Conectar.MVVM.View.ResultadosBusquedaPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Conectar.MVVM.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ResultadosBusquedaPage">

    <Page.Background>
        <ImageBrush ImageSource="pack://application:,,,/Imagenes/Background.png"
                    Stretch="UniformToFill" />
    </Page.Background>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="20">
            <Button VerticalAlignment="Top"
                HorizontalAlignment="Left"
                Style="{StaticResource BotonModerno}" Content="VOLVER"
                Margin="5"
                FontSize="12"
                Width="70"
                Height="20" Click="BotonVolver"
            />
            <TextBlock Text="RESULTADOS DE BÚSQUEDA" Foreground="White" FontSize="20" FontWeight="Bold" Margin="20,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="20,0,20,20">
            <ItemsControl ItemsSource="{Binding Resultados}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Click="Pelicula_Click" Tag="{Binding}" Background="Transparent" BorderBrush="Transparent" Padding="0" Margin="0,0,0,10">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="#1a1a1a" CornerRadius="5" Padding="10" BorderThickness="1" BorderBrush="#333">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="80"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <Border Grid.Column="0" Height="120" CornerRadius="3" ClipToBounds="True">
                                                <Image Source="{Binding ImagenFuente}" Stretch="UniformToFill"/>
                                            </Border>

                                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="20,0,0,0">
                                                <TextBlock Text="{Binding Titulo}" Foreground="White" FontSize="22" FontWeight="Bold"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
